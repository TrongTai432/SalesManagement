@model SV20T1020091.DomainModels.Customer
@{
    ViewBag.Title = "Xóa khách hàng";
}

<div class="box box-danger">
    <div class="box-body">
        <form class="form-horizontal" method="post" action="~/Customer/Delete/@Model.CustomerID">
            <div class="form-group">
                <label class="control-label col-sm-2">Tên khách hàng:</label>
                <div class="col-sm-10">
                    <p class="form-control-static">@Model.CustomerName</p>
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Tên giao dịch:</label>
                <div class="col-sm-10">
                    <p class="form-control-static">@Model.ContactName</p>
                </div>
            </div>


            <div class="form-group">
                <label class="control-label col-sm-2">Điện thoại:</label>
                <div class="col-sm-10">
                    <p class="form-control-static">@Model.Phone</p>
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Email:</label>
                <div class="col-sm-10">
                    <p class="form-control-static">@Model.Email</p>
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Địa chỉ:</label>
                <div class="col-sm-10">
                    <p class="form-control-static">@Model.Address</p>
                </div>
            </div>
            <div class="form-group">
                <label class="control-label col-sm-2">Tỉnh thành:</label>
                <div class="col-sm-10">
                    <p class="form-control-static">@Model.Province</p>
                </div>
            </div>
            
            <div class="form-group">
                <label class="control-label col-sm-2">Trạng thái:</label>
                <div class="col-sm-10">
                    <p class="form-control-static">@(Model.IsLocked ? "Tài khoản đang bị khóa" : "Tài khoản đang hoạt động")</p>
                </div>
            </div>

            

            <div class="form-group">
                @if(!SV20T1020091.BusinessLayers.CommonDataService.IsUsedCustomer(Model.CustomerID))
                {
                    <button type="submit" class="btn btn-danger">
                        <i class="fa fa-floppy-o"></i>
                        Xác nhận xóa
                    </button>
                }
                
                    <a href="~/Customer" class="btn btn-default">
                        Quay lại
                    </a>
                
            </div>

        </form>

    </div>
</div>
